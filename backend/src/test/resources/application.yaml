jwt:
  secret: kargo-jwt-secret-key-deve-ser-alterado-em-producao-minimo-32-chars
  expiration-ms: 86400000

spring:
  datasource:
    url: jdbc:h2:mem:kargo-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

